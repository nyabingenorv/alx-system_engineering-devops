# Firewall

- Configuring `ufw` so that it blocks all incoming traffic, except for the TCP ports:

1. 22 (SSH)
2. 443 (HTTPS SSL)
3. 80 (HTTP)

- Configure server`web-01` so that its firewall redirects port 8080/TCP to port 80/TCP.
